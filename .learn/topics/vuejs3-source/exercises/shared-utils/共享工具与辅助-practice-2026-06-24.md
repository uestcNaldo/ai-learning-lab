# Practice Session - 2026-06-24

## Concept Practiced
- Concept: 共享工具与辅助 | Difficulty: Intermediate | Exercise: shared-utils mini runtime review

## User's Submitted Code
```javascript
function isObject(value) {
  // TODO: 只在 value 是非 null 对象时返回 true。
  return typeof value === 'object' && value !== null
}

function hasChanged(value, oldValue) {
  // TODO: 使用 Object.is 语义。NaN 和 NaN 不应算作变化。
  return !Object.is(value, oldValue)
}

const hasOwnProperty = Object.prototype.hasOwnProperty

function hasOwn(value, key) {
  // TODO: 安全检查 key 是否是 value 自身拥有的属性。
  return hasOwnProperty.call(value, key)
}

function parseStringStyle(cssText) {
  // TODO: 把 "color: red; font-size: 14px;" 转成 { color: "red", "font-size": "14px" }。
  // 这个入门版本保持简单：先按分号拆分，再按每一项里的第一个冒号拆分。
  const cssStyleObj = {}
  const styleArr = cssText.split(';').filter(item => item !== '')
  for (const style of styleArr) {
    const styleItem = style.split(':')
    const key = styleItem[0].trim()
    const value = styleItem[1].trim()
    cssStyleObj[key] = value
  }
  return cssStyleObj
}

function normalizeClass(value) {
  // TODO: 支持 string、array、嵌套 array 和 object 语法。
  // 示例：
  // normalizeClass(["btn", ["primary"], { active: true, hidden: false }])
  // -> "btn primary active"
  if (typeof value === 'string') {
    return value
  }
  if (isObject(value) && !Array.isArray(value)) {
    return Object.entries(value).filter(([k, v]) => v === true || Boolean(v) === true).map(([k, v]) => {
      return k
    }).join(' ')
  }
  if (Array.isArray(value)) {
    return value.map(item => {
      return normalizeClass(item).trim()
    }).join(' ').trim()
  }
  return ''
}

function normalizeStyle(value) {
  // TODO: 支持 string、object 和 array。
  // 顶层 string 应保持 string；只有出现在 array 内部时，才需要解析并合并。
  if (typeof value === 'string') {
    return value
  }

  if (!Array.isArray(value) && isObject(value)) {
    return value
  }

  if (Array.isArray(value)) {
    const result = {}
    for (const item of value) {
      if (typeof item === 'string') {
        const styleObj = parseStringStyle(item)
        Object.assign(result, styleObj)
      }
      if (isObject(item)) {
        Object.assign(result, item)
      }
    }
    return result
  }
}

function normalizeProps(props) {
  // TODO: 传入 null 时返回 null；否则在存在 props.class 和 props.style 时进行标准化。
  if (props === null) {
    return null
  }
  const result = {}
  if (props.class) {
    result.class = normalizeClass(props.class)
  }
  if (props.style) {
    result.style = normalizeStyle(props.style)
  }
  return result
}

function createReactiveGate(value) {
  // TODO: 如果 value 不是对象，原样返回。
  // 如果 value 是对象，返回一个通过 Reflect 支持基础 get/set 的 Proxy。
  if (!isObject(value)) {
    return value
  } else {
    const proxy = new Proxy(value, {
      get (target, prop, receiver) {
        return Reflect.get(target, prop, receiver)
      },
      set (target, prop, newValue, receiver) {
        return Reflect.set(target, prop, newValue, receiver)
      }
    })
    return proxy
  }
}
```

## AI Feedback
你这轮修复抓住了上一轮最关键的两个问题：`hasOwn` 已经改成 `Object.prototype.hasOwnProperty.call(value, key)`，这和 Vue 源码里的兼容写法一致；`normalizeStyle` 也已经支持顶层 object，符合 Vue 中 `normalizeStyle()` 对 string、array、object 三类输入的处理方向。当前练习脚本已经通过全部测试，说明你已经能把 `@vue/shared` 风格工具函数串到一个最小运行链路里。

继续追问一个边界：Vue 源码里的 `normalizeProps(props)` 会在原 props 上标准化 `class` 和 `style`，然后返回 props 本身。你当前版本会新建 `result`，因此 `normalizeProps({ id: 'app', class: ['root'] })` 会丢掉 `id`。这个点不影响当前测试通过，但它会影响你理解“normalize 不是筛选 props，而是把 props 调整成 renderer 更好消费的形态”。

边界检查：`parseStringStyle` 现在是入门版，足够通过本练习；真实 Vue 源码会额外处理 CSS 注释、括号里的分号、以及值里包含冒号的场景。`normalizeClass` 的递归方向是对的，后续可以把 object 分支写得更接近源码：遍历 key，只在对应 value truthy 时拼接 key。

代码质量建议：下一轮只做一个小改动就好，把 `normalizeProps` 改成保留原 props 的语义。你可以思考：如果用户写了 `id`、`onClick`、`data-*`，为什么标准化 `class/style` 时不应该把这些字段丢掉？

## Assessment
- Understanding: Good
- Status: needs_practice → needs_practice | Confidence: 0.25 → 0.3
